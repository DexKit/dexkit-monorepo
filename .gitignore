# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
**/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
**/.next/
/out/
**/out/

# production
/build
**/build
**/dist
/packages/*/dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
# TypeScript build artifacts (compiled files alongside source)
# Ignore all .d.ts and .d.ts.map files (these are generated by TypeScript compiler)
**/*.d.ts
**/*.d.ts.map
# But keep specific declaration files that are source files (not generated)
!apps/**/next-env.d.ts
!apps/**/global.d.ts
# Note: !**/src/types/*.d.ts and !**/types/*.d.ts removed - these are generated by build
# Compiled JavaScript files in packages (generated alongside .ts/.tsx)
packages/**/*.js
packages/**/*.js.map
# Compiled JavaScript files in apps (generated alongside .ts/.tsx)
apps/**/*.js
apps/**/*.js.map
# But keep source .js files (configs, scripts, etc.)
!packages/**/.storybook/**/*.js
!packages/**/scripts/**/*.js
!**/*.config.js
!**/*.config.cjs
!**/scripts/**/*.js
!**/.storybook/**/*.js
# Keep specific source .js files in apps
!apps/**/scripts/**/*.js
!apps/**/public/**/*.js

# turbo
.turbo

# package-lock files (since we're using Yarn)
**/package-lock.json

# Storybook build outputs
**/storybook-static

# cursor params
.cursorrules
.cursorignore
/.cursorrules.backup