{
  "name": "@dexkit/wallet-connectors",
  "private": false,
  "version": "0.8.0",
  "license": "MIT",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "rimraf dist && tsc -b",
    "build:tsup": "rimraf dist && tsup",
    "lint": "TIMING=1 eslint \"**/*.ts*\""
  },
  "dependencies": {
    "@dexkit/core": "*"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-custom": "*",
    "rimraf": "^5.0.5",
    "tsconfig": "*",
    "tsup": "^6.5.0",
    "typescript": "5.8.3"
  },
  "publishConfig": {
    "access": "public"
  }
}